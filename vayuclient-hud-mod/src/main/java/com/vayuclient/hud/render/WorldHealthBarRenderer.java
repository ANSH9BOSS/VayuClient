package com.vayuclient.hud.render;

import net.fabricmc.fabric.api.client.rendering.v1.level.LevelRenderContext;

public class WorldHealthBarRenderer {
    public static void onCollectSubmits(LevelRenderContext context) {
        // Legacy 3D billboard health bar disabled in favor of overhead nametag heart indicator
    }
}
